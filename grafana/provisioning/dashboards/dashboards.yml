apiVersion: 1

providers:
  - name: Agent Observability
    orgId: 1
    folder: Agent Observability
    folderUid: agent-observability
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
